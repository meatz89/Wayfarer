@inherits LocationContentBase

@namespace Wayfarer.Pages.Components

@if (CurrentSpot != null)
{

    <!-- Venue Header -->
    <div class="location-header">
        <div class="location-path">
            @if (!string.IsNullOrEmpty(CurrentLocation?.Name))
            {
                <span>Lower Wards</span>
                <span class="icon-arrow-right"></span>
                <span>@CurrentLocation.Name</span>
            }
        </div>
        <div class="location-name">@CurrentLocation?.Name</div>
        <div class="location-traits">
            <span class="trait">Public</span>
            <span class="trait">@GetTimeOfDayTrait()</span>
        </div>
    </div>

    <!-- Current Spot Banner -->
    <div class="current-spot-banner">
        <div class="current-spot-name">@CurrentSpot.Name</div>
        <div class="spot-traits">
            @foreach (var prop in CurrentSpot.LocationProperties ?? new List<LocationPropertyType>())
            {
                <span class="spot-trait @GetSpotTraitClass(prop)">@GetSpotTraitDisplay(prop)</span>
            }
        </div>
    </div>

    <div class="content-area">
        <!-- Atmosphere -->
        @if (CurrentSpot != null)
        {
            <div class="atmosphere-text">
                <LocationAtmosphereRenderer CurrentLocation="CurrentSpot"
                                             CurrentTime="CurrentTime"
                                             NPCsPresent="NPCsAtSpot" />
            </div>
        }

        <!-- Actions Available Here -->
        @{
            bool hasLocationActions = LocationActions.Any();
            bool hasLocationGoals = AvailableSocialGoals.Any() || AvailableMentalGoals.Any() || AvailablePhysicalGoals.Any();
            bool hasAnyActions = hasLocationGoals || hasLocationActions;
        }

        @if (hasAnyActions)
        {
            <div class="section-header">Actions Available Here</div>
            <div class="actions-grid">
                
                <!-- Mental Goals -->
                @foreach (var goal in AvailableMentalGoals)
                {
                    <div class="goal-action-btn @(goal.IsIntroAction ? "intro-action" : "")" @onclick="async () => await StartMentalGoal(goal)">
                        <div class="goal-system-badge mental">Mental</div>
                        <div class="goal-name">@goal.Name</div>
                        @if (!string.IsNullOrEmpty(goal.Description))
                        {
                            <div class="goal-description">@goal.Description</div>
                        }
                        @if (!string.IsNullOrEmpty(goal.InvestigationId))
                        {
                            @if (goal.IsIntroAction)
                            {
                                <div class="goal-investigation-tag intro-tag">🔍 Begin Investigation</div>
                            }
                            else
                            {
                                <div class="goal-investigation-tag">Investigation Goal</div>
                            }
                        }
                    </div>
                }

                <!-- Physical Goals -->
                @foreach (var goal in AvailablePhysicalGoals)
                {
                    <div class="goal-action-btn @(goal.IsIntroAction ? "intro-action" : "")" @onclick="async () => await StartPhysicalGoal(goal)">
                        <div class="goal-system-badge physical">Physical</div>
                        <div class="goal-name">@goal.Name</div>
                        @if (!string.IsNullOrEmpty(goal.Description))
                        {
                            <div class="goal-description">@goal.Description</div>
                        }
                        @if (!string.IsNullOrEmpty(goal.InvestigationId))
                        {
                            @if (goal.IsIntroAction)
                            {
                                <div class="goal-investigation-tag intro-tag">🔍 Begin Investigation</div>
                            }
                            else
                            {
                                <div class="goal-investigation-tag">Investigation Goal</div>
                            }
                        }
                    </div>
                }

                <!-- Dynamic Venue Actions -->
                @foreach (var action in LocationActions)
                {
                    <div class="action-card @(!action.IsAvailable ? "unavailable" : "") @(action.ActionType == "travel" ? "travel" : "")" 
                         @onclick="async () => { if (action.IsAvailable) await ExecuteLocationAction(action); }">
                        <div class="action-name">@action.Title</div>
                        <div class="action-cost">
                            @if (action.IsAvailable)
                            {
                                <span class="cost-value">@action.Cost</span>
                            }
                            else
                            {
                                <span class="unavailable-reason">Cannot perform action</span>
                            }
                        </div>
                        @if (!string.IsNullOrEmpty(action.Detail))
                        {
                            <div class="action-detail">@action.Detail</div>
                        }
                        @if (!string.IsNullOrEmpty(action.InvestigationLabel))
                        {
                            <div class="investigation-label" style="color: @GetInvestigationColor(action.InvestigationLabel)">
                                @action.InvestigationLabel
                            </div>
                        }
                    </div>
                }
            </div>
        }

        <!-- Obstacles at This Location -->
        @if (CurrentObstacles != null && CurrentObstacles.Any())
        {
            <div class="section-header">Obstacles at This Location</div>
            <div class="obstacles-container">
                @foreach (var obstacle in CurrentObstacles)
                {
                    <ObstacleDisplay Obstacle="@obstacle" />
                }
            </div>
        }

        <!-- People at This Spot -->
        @if (AvailableNpcs.Any())
        {
            <div class="section-header">People at This Spot</div>
            @foreach (var npc in AvailableNpcs)
            {
                <div class="npc-card">
                    <div class="npc-header">
                        <div>
                            <div class="npc-name">
                                @npc.Name
                            </div>
                            <div class="npc-type">@GetNPCPersonalityDescription(npc)</div>
                        </div>
                        <div class="npc-state @GetStateClass(npc.ConnectionState)">@npc.ConnectionState</div>
                    </div>
                    <div class="npc-tokens">
                        <div class="token-display">
                            Trust: <span class="token-count">@GetTokenCount(npc.Id, ConnectionType.Trust)</span> 
                            <span class="token-effect">(@GetTokenEffect(npc.Id, ConnectionType.Trust))</span>
                        </div>
                        <div class="token-display">
                            Diplomacy: <span class="token-count">@GetTokenCount(npc.Id, ConnectionType.Diplomacy)</span>
                        </div>
                        <div class="token-display">
                            Status: <span class="token-count">@GetTokenCount(npc.Id, ConnectionType.Status)</span>
                        </div>
                        <div class="token-display">
                            Shadow: <span class="token-count">@GetTokenCount(npc.Id, ConnectionType.Shadow)</span>
                        </div>
                    </div>
                    <div class="npc-description">
                        @GetNPCDescription(npc)
                    </div>
                    <div class="npc-actions">

                        <!-- Social Goals from Location -->
                        @foreach (var goal in AvailableSocialGoals.Where(g => g.NpcId == npc.Id))
                        {
                            <div class="goal-action-btn @(goal.IsIntroAction ? "intro-action" : "")" @onclick="async () => await StartSocialGoal(goal)">
                                <div class="goal-system-badge social">Social</div>
                                <div class="goal-name">@goal.Name</div>
                                @if (!string.IsNullOrEmpty(goal.Description))
                                {
                                    <div class="goal-description">@goal.Description</div>
                                }
                                @if (!string.IsNullOrEmpty(goal.InvestigationId))
                                {
                                    @if (goal.IsIntroAction)
                                    {
                                        <div class="goal-investigation-tag intro-tag">🔍 Begin Investigation</div>
                                    }
                                    else
                                    {
                                        <div class="goal-investigation-tag">Investigation Goal</div>
                                    }
                                }
                            </div>
                        }

                        <!-- NPC Active Goals (Direct from NPC) -->
                        @{
                            var actualNpc = NPCsAtSpot.FirstOrDefault(n => n.ID == npc.Id);
                            if (actualNpc?.ActiveGoals != null)
                            {
                                foreach (var goal in actualNpc.ActiveGoals)
                                {
                                    <div class="npc-action" @onclick="async () => await StartConversationWithRequest(npc.Id, goal.Id)">
                                        <span class="action-type">@goal.Name</span>
                                        <span class="action-details">
                                            <span class="doubt-calculation">@GetDoubtDisplay(npc, CurrentSpot!)</span>
                                        </span>
                                    </div>
                                }
                            }
                        }

                        <!-- Exchange Option if NPC has exchange cards -->
                        @{
                            var npcForExchange = NPCsAtSpot.FirstOrDefault(n => n.ID == npc.Id);
                            if (npcForExchange?.HasExchangeCards() == true)
                            {
                                <div class="npc-action exchange" @onclick="async () => await StartExchange(npc.Id)">
                                    <span class="action-type">Quick Exchange</span>
                                    <span class="action-details">
                                        <span class="doubt-calculation">@GetDoubtDisplay(npc, CurrentSpot!)</span>
                                    </span>
                                </div>
                            }
                        }
                    </div>
                </div>
            }
        }

        <!-- Other Spots -->
        @if (AvailableSpots.Any())
        {
            <div class="section-header">Other Spots in @CurrentLocation?.Name</div>
            <div class="spots-grid">
                @foreach (var spot in AvailableSpots)
                {
                    var isCurrentSpot = spot.Id == CurrentSpot?.Name;
                    <div class="spot-card @(isCurrentSpot ? "current" : "")"
                         @onclick="async () => { if (!isCurrentSpot) await MoveToSpot(spot.Id); }">
                        <div class="spot-card-name">@spot.Name</div>
                        @{
                            var npcsAtSpot = GetNPCsAtSpot(spot.Id);
                            if (npcsAtSpot.Any())
                            {
                                <div class="spot-occupants">
                                    @foreach (var npc in npcsAtSpot)
                                    {
                                        <span class="npc-at-spot">
                                            @npc.Name
                                            <span class="npc-state">(@GetNPCStateDisplay(npc))</span>
                                        </span>
                                        @if (npc != npcsAtSpot.Last())
                                        {
                                            <span>, </span>
                                        }
                                    }
                                </div>
                            }
                        }
                        @if (isCurrentSpot)
                        {
                            <div class="you-are-here">You are here</div>
                        }
                        else
                        {
                            <div class="spot-move-action">Move here (instant, free)</div>
                        }
                    </div>
                }
            </div>
        }
    </div>
}
else
{
    <div class="loading">
        Loading location...
    </div>
}