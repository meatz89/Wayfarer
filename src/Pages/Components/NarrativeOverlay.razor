@* Legacy NarrativeOverlay component - replaced with conversation system *@
@* This is a stub component that does nothing - the narrative system has been replaced *@

@code {
    [Parameter] public bool IsMinimizable { get; set; } = false;
    
    // Stub component - no functionality needed as narrative system has been replaced with conversations
}