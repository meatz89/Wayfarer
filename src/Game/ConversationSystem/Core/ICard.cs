/// <summary>
/// Marker interface for all card types.
/// Allows CardDeck to hold different card types without requiring shared properties.
/// </summary>
public interface ICard
{
    // Empty marker interface - no properties or methods
    // Each card type implements its own specific properties
}