public enum ConditionTypes
{
    SkillLevel,        // Requires specific skill level
    FirstDiscovery,    // Only applies on first discovery
    NodeState,         // Depends on resource node state
    ProgressiveAction  // Depends on how many times action performed
}