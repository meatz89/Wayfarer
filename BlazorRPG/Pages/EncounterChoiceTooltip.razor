@inherits EncounterChoiceTooltipBase
@if (hoveredChoice != null)
{
    <div class="tooltip" style="top:@tooltipYpx; left:@tooltipXpx;">
        <!-- Choice Header -->
        <div class="tooltip-header">
            <h4>@hoveredChoice.NarrativeText</h4>
        </div>

        <!-- Narrative Description -->
        <div class="tooltip-section narrative-section">
            Success: <div class="choice-narrative">@hoveredChoice.SuccessNarrative</div>
            Failure: <div class="choice-narrative">@hoveredChoice.FailureNarrative</div>
        </div>

        <!-- Costs Section -->
        <div class="tooltip-section costs-section">
            <h5>Costs</h5>
            <div class="tooltip-item">
                <div class="resource-costs">
                    @if (hoveredChoice.FocusCost > 0)
                    {
                        <div class="resource-cost focus-cost">
                            <span class="resource-icon">⚡</span> @hoveredChoice.FocusCost Focus
                        </div>
                    }
                    else
                    {
                        <div class="resource-cost focus-cost">
                            <span class="resource-icon">⚡</span> 0 Focus (Recovery Option)
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- Effects Section -->
        <div class="tooltip-section effects-section">
            <h5>Potential Effects</h5>
            <div class="tooltip-item">
                @{
                    var projection = Preview;
                    var positiveEffects = GetPositiveEffectsAsList(hoveredChoice, projection);
                }
                @if (positiveEffects.Any())
                {
                    <div class="effect-list positive-effects">
                        @foreach (var effect in positiveEffects)
                        {
                            <div class="effect-item positive">
                                <span class="effect-icon">
                                    @if (effect.IsProgressEffect)
                                    {
                                        <span>🏆</span>
                                    }
                                    else if (effect.IsFocusEffect)
                                    {
                                        <span>⚡</span>
                                    }
                                    else
                                    {
                                        <span>✓</span>
                                    }
                                </span>
                                @effect.Description
                            </div>
                        }
                    </div>
                }
                else
                {
                    <div class="effect-item neutral">No significant effects</div>
                }
            </div>
        </div>

        <!-- Skill Check Section -->
        @if (hoveredChoice.SkillOption != null)
        {
            <div class="tooltip-section skill-section">
                <h5>Skill Checks</h5>
                <div class="tooltip-item">
                    <div class="skill-details">
                        <div class="skill-name">
                            @hoveredChoice.SkillOption.SkillName Check (@hoveredChoice.SkillOption.Difficulty)
                        </div>
                        <div class="skill-difficulty">
                            Difficulty: @hoveredChoice.SkillOption.SCD
                        </div>
                        <div class="skill-outcome">
                            <div class="success-outcome">
                                <span class="outcome-label">Success:</span>
                                <span class="outcome-description">@hoveredChoice.SkillOption.SuccessEffectEntry?.Effect ?? "No Effect"</span>
                            </div>
                            <div class="failure-outcome">
                                <span class="outcome-label">Failure:</span>
                                <span class="outcome-description">@hoveredChoice.SkillOption.FailureEffectEntry?.Effect ?? "No Effect"</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}