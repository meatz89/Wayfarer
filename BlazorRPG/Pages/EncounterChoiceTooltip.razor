@inherits EncounterChoiceTooltipBase
<div class="tooltip" style="top:@tooltipYpx; left:@tooltipXpx;">
    <!-- Choice Header -->
    <div class="tooltip-header">
        <h4>@hoveredChoice.Choice.NarrativeText</h4>
    </div>

    <!-- Narrative Description -->
    <div class="tooltip-section narrative-section">
        <div class="choice-narrative">@hoveredChoice.ChoiceDescription</div>
    </div>

    <!-- Costs Section -->
    <div class="tooltip-section costs-section">
        <h5>Costs</h5>
        <div class="tooltip-item">
            <div class="resource-costs">
                @if (hoveredChoice.Choice.FocusCost > 0)
                {
                    <div class="resource-cost focus-cost">
                        <span class="resource-icon">⚡</span> @hoveredChoice.Choice.FocusCost Focus
                    </div>
                }
                else
                {
                    <div class="resource-cost focus-cost">
                        <span class="resource-icon">⚡</span> 0 Focus (Recovery Option)
                    </div>
                }
            </div>
        </div>
    </div>

    <!-- Effects Section -->
    <div class="tooltip-section effects-section">
        <h5>Potential Effects</h5>
        <div class="tooltip-item">
            @{
                var projection = Preview;
                var positiveEffects = GetPositiveEffectsAsList(hoveredChoice.Choice, projection);
            }
            @if (positiveEffects.Any())
            {
                <div class="effect-list positive-effects">
                    @foreach (var effect in positiveEffects)
                    {
                        <div class="effect-item positive">
                            <span class="effect-icon">
                                @if (effect.IsProgressEffect)
                                {
                                    <span>🏆</span>
                                }
                                else if (effect.IsFocusEffect)
                                {
                                    <span>⚡</span>
                                }
                                else
                                {
                                    <span>✓</span>
                                }
                            </span>
                            @effect.Description
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="effect-item neutral">No significant effects</div>
            }
        </div>
    </div>

    <!-- Skill Check Section -->
    @if (hoveredChoice.Choice.SkillOptions.Count > 0)
    {
        <div class="tooltip-section skill-section">
            <h5>Skill Checks</h5>
            @foreach (var skillOption in hoveredChoice.Choice.SkillOptions)
            {
                <div class="tooltip-item">
                    <div class="skill-details">
                        <div class="skill-name">
                            @skillOption.SkillName Check (@skillOption.Difficulty)
                        </div>
                        <div class="skill-difficulty">
                            Difficulty: @skillOption.SCD
                        </div>
                        <div class="skill-outcome">
                            <div class="success-outcome">
                                <span class="outcome-label">Success:</span>
                                <span class="outcome-description">@skillOption.SuccessPayload.NarrativeEffect</span>
                            </div>
                            <div class="failure-outcome">
                                <span class="outcome-label">Failure:</span>
                                <span class="outcome-description">@skillOption.FailurePayload.NarrativeEffect</span>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }

    <!-- State Flags Section -->
    <div class="tooltip-section flags-section">
        <h5>Encounter Stage</h5>
        <div class="tooltip-item">
            <div class="stage-info">
                @hoveredChoice.Index
            </div>
        </div>
    </div>
</div>